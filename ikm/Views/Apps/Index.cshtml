@model IEnumerable<ikm.Models.App>

@{
    ViewData["Title"] = "Справочник приложений";
}

<h1>Справочник приложений</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fa-solid fa-plus"></i> Добавить приложение
    </a>
</p>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Имя процесса (exe)</th>
            <th>Название приложения</th>
            <th style="width: 150px;">Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.ProcessName)</td>
                <td>@Html.DisplayFor(modelItem => item.BaseName)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ProcessName" class="btn btn-primary btn-sm" title="Редактировать">
                        <i class="fa-solid fa-pen-to-square"></i>
                    </a>
                    <a asp-action="Delete" asp-route-id="@item.ProcessName" class="btn btn-danger btn-sm" title="Удалить">
                        <i class="fa-solid fa-trash"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>