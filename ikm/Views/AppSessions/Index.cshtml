@model IEnumerable<ikm.Models.AppSession>

@{
    ViewData["Title"] = "Посещения приложений";
}

<h1>Посещения приложений</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fa-solid fa-plus"></i> Добавить запись
    </a>
</p>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Дата начала</th>
            <th>Время начала</th>
            <th>Дата окончания</th>
            <th>Время окончания</th>
            <th>Приложение</th>
            <th>Заголовок окна</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ActivationDate</td>
                <td>@item.ActivationTime</td>
                <td>@item.ShutdownDate</td>
                <td>@item.ShutdownTime</td>
                <td>@Html.DisplayFor(modelItem => item.ProcessName)</td>
                <td>@Html.DisplayFor(modelItem => item.WindowTitle)</td>
                <td>
                    <div class="d-flex gap-2">
                        <a asp-action="Edit" asp-route-id="@item.SessionId" class="btn-table-edit" title="Редактировать">
                            <i class="fa-solid fa-pen-to-square"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.SessionId" class="btn-table-delete" title="Удалить">
                            <i class="fa-solid fa-trash"></i>
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>